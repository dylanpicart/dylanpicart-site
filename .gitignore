# Build output — regenerated by the deploy workflow, never commit
dist/
.astro/

# Dependencies — installed fresh by npm install in CI
node_modules/

# Environment variables — never commit secrets
.env
.env.*
!.env.example

# OS noise
.DS_Store
Thumbs.db

# Editor noise
.vscode/settings.json
.idea/

# Formspree, API keys, or any local config you add later
*.local